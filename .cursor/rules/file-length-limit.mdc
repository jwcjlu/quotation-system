---
description: 单文件代码行数限制（<=300行）
alwaysApply: true
---

# 单文件长度限制

- 默认要求：每个代码文件不超过 300 行。
- 超过 300 行时，优先拆分为更小模块（按职责拆分函数、结构体、服务或仓储）。
- 例外场景：
  - 自动生成文件（如 `*.pb.go`、`wire_gen.go`）
  - 数据库 migration SQL
  - 必要的第三方协议/数据映射大文件
- 发生例外时，需在评审说明无法拆分的原因。
