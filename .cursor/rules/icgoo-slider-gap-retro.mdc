---
description: ICGOO 滑块缺口不准时复盘要点；阿里云拼图与易盾差异、勿信单一置信度、验证命令
globs:
  - scripts/icgoo*.py
  - scripts/icgoo_aliyun*.py
alwaysApply: false
---

# ICGOO 滑块缺口 / 阿里云拼图 — 复盘约定

## 触发

用户或日志反映：**拖动距离调了仍偏、或 dddd/OpenCV 给出的 raw_x 与肉眼不符**。

## 易错认知（勿重复）

- **高 confidence / 两路只差几像素 ≠ 缺口真值**：`slide_match` 在纹理重复、阴影、圆角、阿里云**灰底空洞**与**带纹理拼图块**视觉不一致时，多峰仍可能整体偏移。
- **缺口不准 vs 拖动不准**：先区分图内 `raw_x`（`00_detect.txt` / `aliyun_calibration.jsonl` 的 `gap_detected`）与页面 `drag_scale_x`、阿里云加量；只调 `--drag-boost` 无法修正错误峰。
- **改 dev 分支变量作用域**：任何「仅在 `if distance_alt` 内赋值」的变量，若后面 `det_extra` / 写文件仍引用，须在外层先赋值（曾出现 `xg referenced before assignment`）。

## 推荐排查顺序

1. **看图**：`--captcha-debug-dir` 下 `00_background.png` / `00_slider.png`；肉眼标定真值 x，与 `raw_x`、`opencv_x_*`、`dddd_raw_x_*` 对比。
2. **离线**：`scripts/benchmark_aliyun_gap.py --dataset-root <样本库>`（配合 `--aliyun-dataset-root` 落盘样本）。
3. **时间线**：同次 run 的 `aliyun_calibration.jsonl` 中 `event=gap_detected` 与 `slide_attempt` 对照。
4. **仅阿里云**：`--aliyun-drag-scale-mult` / `--aliyun-systematic-extra-px` 或对应环境变量；易盾不要共用误调。

## 代码改动原则

- 阿里云专用逻辑放在 `icgoo_aliyun_gap.py` / `icgoo_aliyun_captcha.py`；`detect_gap` / `slide_verification` 与生产对齐，避免 `icgoo_crawler_dev` 再复制一份核心公式。

## 验证

- `python -m py_compile scripts/icgoo_crawler_dev.py scripts/icgoo_yidun_solver.py`
- 跑一次带 `--captcha-debug-dir` 的 dev，确认无异常且 `00_detect.txt`、JSONL 写入完整。
